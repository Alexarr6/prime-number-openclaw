# Progress Log
Run: d355398f-8b3a-4078-8f83-56449ece6671
Task: Crear un programa en Python que indique si un número entero es primo o no.
Started: 2026-02-20T21:27:00Z

## Codebase Patterns
- Las pruebas se ejecutan con `python3 -m unittest -v` usando `unittest` de la stdlib.
- Para tests de CLI en stdlib, usar `subprocess.run` con `sys.executable` y `capture_output=True`.
- `argparse` retorna código de salida `2` cuando falla el parseo de argumentos; útil para tests de CLI inválida.
- El chequeo rápido de sintaxis/typecheck en este proyecto se valida con `python3 -m py_compile`.

---

## 2026-02-20T21:28:00Z - US-001: Crear módulo primo.py con función es_primo(n)
- Se validó la implementación de `es_primo(n: int) -> bool` en `primo.py` con manejo de bordes (`n < 2`, `2`, pares > 2) y evaluación de impares hasta raíz cuadrada.
- Se ajustaron pruebas de `test_primo.py` para incluir explícitamente el caso `es_primo(4) == False` solicitado por aceptación.
- Se ejecutaron pruebas unitarias y validación de compilación.
- Files changed: `test_primo.py`, `progress-d355398f-8b3a-4078-8f83-56449ece6671.txt`.
- **Learnings:** Para criterios explícitos conviene reflejar exactamente los valores de aceptación en tests.
---

## 2026-02-20T21:33:00Z - US-002: Agregar CLI con argparse y salida exacta primo/no primo
- Se validó la CLI en `primo.py` con `argparse` y salida exacta `primo` / `no primo` sin texto adicional.
- Se agregaron pruebas de comportamiento CLI en `test_primo.py` usando `subprocess` para los casos `2`, `15` y `-7`.
- Se mantuvieron las pruebas de la función `es_primo` y se confirmó compatibilidad de ejecución como script.
- Files changed: `test_primo.py`, `progress-d355398f-8b3a-4078-8f83-56449ece6671.txt`.
- **Learnings:** Usar `Path(__file__).with_name("primo.py")` hace robusta la invocación del script en tests CLI.
---

## 2026-02-20T21:38:00Z - US-003: Crear test_primo.py (>=8 tests) y preparar entrega con PR
- Se reforzó `test_primo.py` con una prueba adicional de regresión de CLI para argumento inválido (`abc`), validando parseo de `argparse` con exit code 2.
- Se confirmó cobertura de tests para casos requeridos `2`, `15` y `-7`, y suite total >=8 casos.
- Se ejecutaron pruebas (`unittest`) y validación de sintaxis (`py_compile`).
- Se preparó la entrega para PR desde `feature/primo-cli-tests` hacia `main`.
- Files changed: `test_primo.py`, `progress-d355398f-8b3a-4078-8f83-56449ece6671.txt`.
- **Learnings:** además de salida exacta, conviene testear comportamiento de error de CLI para evitar regresiones de parsing.
---
